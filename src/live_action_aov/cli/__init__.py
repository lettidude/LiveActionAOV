"""Typer CLI surface.

The full command set (discover / analyze / run / run-shot / preflight /
plugins / models) lands in Phase 4; Phase 0 ships just enough to hit the
exit criterion: version, plugins list, and run-shot --passes noop.
"""
