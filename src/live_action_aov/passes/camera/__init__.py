"""Camera pass — v1 stub only.

Full implementation lands in v2a (design §20). The stub declares
`pass_type = "camera"` and a non-EXR `SidecarSpec` so the sidecar writer
registry flows through non-EXR outputs from day one (§20.10).
"""
